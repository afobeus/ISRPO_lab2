def area(a, h):
    """Returns area of a triangle with given sides.
            Parameters:
                a (int): length of a side
                h (int): length of height passed to the side a"""
    return a * h / 2 


def perimeter(a, b, c): 
    return a + b + c 
